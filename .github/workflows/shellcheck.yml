on:
  pull_request:
    branches:
      - "*"
  push:
    branches:
      - "main"
name: shellcheck
jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: run shellcheck
        uses: ludeeus/action-shellcheck@master
